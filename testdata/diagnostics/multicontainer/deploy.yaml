apiVersion: apps/v1
kind: Deployment
metadata:
  name: multicontainer
spec:
  replicas: 1
  selector:
    matchLabels: { app: multicontainer }
  template:
    metadata:
      labels: { app: multicontainer }
    spec:
      containers:
        - name: web
          image: busybox:1.36
          command: ["sleep", "3600"]
        - name: sidecar
          image: busybox:1.36
          command: ["sh", "-c"]
          args: ['echo "sidecar: failed to read /etc/config/token: no such file"; exit 3']
