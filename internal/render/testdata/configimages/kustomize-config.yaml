# Extends the images transformer to a CRD's nested image path, which the builtin
# field specs do not cover. `templates` is a list; the field spec walker iterates
# it without a `[]` suffix (it descends real sequences).
images:
  - kind: Pipeline
    path: spec/templates/container/image
