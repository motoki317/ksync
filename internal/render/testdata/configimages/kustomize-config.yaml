# Extends the images transformer to the argo WorkflowTemplate CRD image path,
# which the builtin field specs do not cover. `templates` is a list; the field
# spec walker iterates it without a `[]` suffix (it descends real sequences).
images:
  - kind: WorkflowTemplate
    path: spec/templates/container/image
