apiVersion: batch/v1
kind: CronJob
metadata:
  name: api-b-report
  namespace: team-a
spec:
  schedule: "0 3 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: report
              image: example.com/team-a/api-b
